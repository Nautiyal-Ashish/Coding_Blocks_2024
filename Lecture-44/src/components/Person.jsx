function Person() {
    // Statements => (for,if,else,clg,while,let)
    let a = 10;

    // Expressions => (ternary,map,filter)
    return (
        <div>
            <h1>Person - {a}</h1>
        </div>
    )
}


export default Person

// { } = used to add any data in between the html tags