// importing all files (cheeta , sher , jaadu)
const cheeta = require('./cheeta')
const sher = require('./sher')
const jaadu = require('./jaadu')

// exporting the files to app.js
module.exports = { cheeta, sher, jaadu }